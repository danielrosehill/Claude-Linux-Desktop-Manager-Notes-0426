# refined/

Inbox notes cleaned up by Claude: fixed transcription errors, structured into sections, preserved intent. The source inbox file is linked in frontmatter.

Claude should not add opinion or new content here — that belongs in `analysis/`.
