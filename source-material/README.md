# source-material/

Reference copies of pre-existing work that this notes repo builds on.

## `linux-desktop-plugin/`

Snapshot of Daniel's existing Claude Code plugin at `~/repos/github/my-repos/linux-desktop-plugin/` — 275 slash commands already mapped. This is the starting point for the module definitions in `modules.md`: the commands reveal which buckets have already been thought through (namespace prefixes like `debugging-`, `package-management-`, `fs-optimisation-`, `network-lan-`, etc.) and which are still ad-hoc.

Treat it as read-only reference. Refinements and new structure land in `modules.md`, `refined/`, `analysis/`, `workflows/`.
